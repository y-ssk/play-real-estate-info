# === 秘匿情報（コミット禁止）===
# キーは リポジトリ外 ~/.config/config.env に置く（AI参照禁止）。リポジトリには置かない。
# 以下は「万一リポジトリ内に作られた場合」の belt-and-suspenders
.env
*.env
config.env
secret.yaml
secret.*.yaml

# === Go ===
/bin/
*.test
*.out
*.exe

# === OS / エディタ ===
.DS_Store
*.swp
.idea/
.vscode/

# === ローカル個別 ===
*.local
CLAUDE.local.md
