# <ID>. <APIの正式名称>

> MLIT 不動産情報ライブラリ API の IF 定義（公式マニュアル / IF 定義を**転記**したもの）。
> **憶測で埋めない。** 原文で確認できない項目は「未確認」と明記する。
> 実装着手前に本書を最終確認する（`docs/99_decision-register.md`「各MLIT APIのIF定義の最終確認」）。

## 1. 概要
- 振り分け: ETL / 両用 / タイル配信（`docs/02_architecture.md` 第3節）
- 形式: JSON / 点GeoJSON / GISデータ
- 取得粒度: 市区町村 / 点 / 区域 など
- エンドポイント: `https://www.reinfolib.mlit.go.jp/ex-api/external/<ID>?<パラメータ>`
- 認証: ヘッダ `Ocp-Apim-Subscription-Key: <APIキー>`（キーは環境変数 `MLIT_API_KEY`。**AIは値に触れない**）
- エンコード: gzip（`--compressed` / クライアントでのデコード要否を記す）

### URL（転記元・参照先）
- 公式ドキュメント（転記元URL）: <この IF 定義を写したマニュアルページの URL>
- 仕様内で参照される外部URL（コード一覧・国土数値情報など。仕様本文に出てきたら全て控える）:
  - <URL> … <用途（例: 駅コード N02、都道府県コード一覧 等）>

## 2. リクエストパラメータ
| パラメータ | 内容 | 形式 | 必須 | 備考 |
|---|---|---|---|---|
|  |  |  |  |  |

制約（※注記）:
-

## 3. レスポンス（出力）
包み構造（実際の形を転記。例: `{ "status": "OK", "data": [ ... ] }`）:

| タグ名 | 内容 | データ型 | データ例 | 備考 |
|---|---|---|---|---|
|  |  |  |  |  |

## 4. 使用例
リクエスト:
```
curl -H "Ocp-Apim-Subscription-Key:{APIキー}" --compressed "<URL>" > <ID>.json
```
レスポンス例:
```
（原文の出力例を転記）
```

## 4.5 コード定義（凡例）
> コード→名称の対応表（用途地域区分・液状化段階・防災の整数区分など）。色分け表示と層1検証に使う。
> **原典の貼り付けはオーナー、構造化はAI。憶測でコード→名称を作らない**（無ければ「未収集」と明記）。
> 多くは原典ページ依存（上の「参照される外部URL」に出所を控える）。レイヤー実装着手時にJITで埋めてよい。

- 出所URL: <コード一覧の原典URL / 「インライン記載」/「未収集」>
- 対応表:

| コード | 名称 | 備考 |
|---|---|---|
|  |  |  |

## 5. MachiLens 取り込み観点（実装・レビューの足場）
> ここは公式転記ではなく**本プロダクト向けの設計メモ**。実装役・レビュー役が使う（`docs/04` 第5節 層1）。
> 着手段階で埋める。空欄は「未検討」。

- 取り込み対象フィールド / PostGIS マッピング:
- 一意キー（冪等性の判定キー）:
- 値域・整合チェック（層1。あり得ない値の例＝負の価格・率100%超など）:
- 欠損の扱い（"データなし" の判定と UI 表示への接続）:
- 出典の保持（MLIT 出典の紐づけ＝法的要件）:
- レート制御メモ（連続実行を避ける）:
- 未確認・要確認事項:
