# 記録様式（ADR・学習メモ）の入口

> 参照チェーン：`CLAUDE.md` → 本書 → スキル `record-format`。
>
> 様式の**実体はスキル `record-format`** に隠蔽（`.claude/skills/record-format/SKILL.md`）。
> 設計判断を残すとき・理解を記録するときは、そのスキルを参照して様式に沿わせること。
>
> - **ADR 実体**：`docs/adr/`（1決定1ファイル・4桁連番。`99_decision-register.md`＝索引、`adr/`＝詳細）
> - **学習メモ実体**：`docs/notes/`（`YYYY-MM-DD-topic.md`）
> - **コミット/PR 様式**：スキル `commit-pr-format`（`docs/commit-pr-format.md` 経由）
>
> 確定（Step D）。
