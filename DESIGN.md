# DESIGN.md — MachiLens UI 憲法

> melta-ui（MIT）の設計仕様書を MachiLens リポジトリに取り込み、固有の差分を被せたもの。
> melta-ui はコンポーネントライブラリではなく「設計仕様書」のため、思想・トークン体系・コンポーネント定義は原則そのまま活かす（差分主義で上書きする）。
> 取り込み部分には melta-ui の MIT 著作権表示・ライセンス文を保持する（MITの条件）。
> melta-ui は器非依存のため、フロントのフレームワーク確定とは独立して使える。
> 本書は UI の憲法。技術構成は `02_architecture.md`、スコープは `01_project-overview-scope.md`。
> AIエージェント（FE役）が UI を生成するとき最初に読むファイル。参照するのは常に本ファイル（自リポジトリ内）。

---

## 0. melta-ui からの継承（変更しない）

melta-ui の以下はそのまま採用する。再定義しない。

- **思想**：Quiet Precision／Breathable／Flat & Layered／Subtle Warmth、「UIは黒子」
- **非交渉原則**：Content First、WCAG 2.1 AA、Semantic Color、3-Color Rule、4px Grid、Minimal Elevation、No AI-ish Decoration（カラーバー禁止・全周ボーダー）
- **トークン体系**：spacing（4px grid）、radius、elevation、motion、z-index、タイポのサイズ段（body/body-sm/caption 等）
- **コンポーネント定義**：button/card/input/select/table/nav 等の Quick Reference クラス

> melta-ui の思想（静かに正確に伝える）は MachiLens（街の素性を静かに正確に伝える）と整合しているため、思想レベルの改変は行わない。

---

## 1. 色 — MachiLens 固有（melta の primary を上書き）

地図プロダクト特有の事情：UI色とデータ色（地図レイヤー）が衝突する。**データ色の社会的慣例を守り、UI色を引かせる**。

### データ色（地図レイヤー）— 慣例を固定、最優先で保護
- 浸水想定 = **青系**（ハザードマップ慣例）
- 土砂災害 = **暖色（黄〜赤）**（慣例）
- 人口・相場 = 段階色／グラデーション（青・暖色と被らない系統）

→ **UIの操作色を青にしない**（浸水と誤認するため）。melta既定の primary（青 #2b70ef）はこの理由で上書きする。

**面塗り指標の色相体系（`ADR-0032`）**：面塗りはトグルで1指標ずつ表示するため、切替時に「今どの指標(分野)か」を色相で識別できるよう、次の優先順位で色相を割り当てる。**具体割り当ては層4目視で評価・確定（暫定調整あり）**。

1. 社会的慣例色が最優先（誤認防止）：浸水(洪水/津波)=青／土砂=暖色(黄〜赤)／液状化=慣例が定まらず体系側で別途割当。
2. 慣例のない量的指標は分野で色相を束ねて識別：相場=緑／将来=紫系（人口増減=発散 紫↔緑・高齢化=紫 逐次）／基盤(面積)=灰。
3. 各指標の中は単色の濃淡＝量の大小のみ（発散は符号付き指標のみ）＝色に良し悪しを込めない（`ADR-0005`）。
4. 予約色を侵さない：青=水害／コーラル=UI操作／スレート・near-black=境界/ホバー／白=絞り込みスクリム。
5. 色覚配慮（ColorBrewer 単色/発散系）。重ね系（点/線オーバーレイ）は別体系（重ね機構の時に定義）。

| 分野 | 面塗り指標 | 方式 | 色相 |
|---|---|---|---|
| 災害(浸水) | 洪水/津波 該当面積率 | 逐次 | 青 |
| 災害(土砂) | 土砂 該当面積率 | 逐次 | 暖色(黄〜赤) |
| 災害(液状化) | 液状化 該当面積率 | 逐次 | 要確認（体系側で別途） |
| 相場 | 地価中央値 | 逐次 | 緑 |
| 将来 | 人口増減率 | 発散 | 紫↔緑（PRGn） |
| 将来 | 高齢化率 | 逐次 | 紫（Purples） |
| 基盤 | 面積 | 逐次 | 灰（Greys） |

### UI色
- **ベース（黒子）＝スレート系**：背景・テキスト・枠・非アクティブ要素。画面の大半。
- **差し色（操作の一点）＝コーラル**（`ADR-0027` で hex 確定）：押せるボタン・選択中・「ここを操作して」の一点にだけ、狭い面積で使う。**brand/action の2段スケール**で持つ：
  - **action `#D6443A`**（濃い）＝白文字が乗る面（ボタン・選択中チップ）。白文字コントラスト ≈ 4.5:1 ＝ WCAG AA 適合。
  - **brand `#F2615A`**（鮮やか）＝文字を載せない強調（選択リング・フォーカス・地図ピン・**地図の選択中の区の輪郭**）。
- 原則：**衝突は “面で重なる” ときに起きるので、差し色は “点” で使う**。melta の 3-Color Rule（背景・アクセント・テキスト）と一貫。
- **地図の選択輪郭＝コーラル brand**（スライス3.6・`mapTokens.ts`）：選択中の1区だけの細い輪郭（＝点）に brand を使い「いま選んでいる区」を確定として示す。線は1区分で面を塗らないため青（浸水）と衝突しない。**ホバー（一時）は中立 near-black の線＋淡い面オーバーレイ**で示し、色で確定（コーラル）と一時（中立）を割る。

> コーラルは候補B（クラシック）で確定（`ADR-0027`・2段スケールの根拠と代替案の棄却理由はそちら）。暖色（オレンジ寄り）は土砂災害のデータ色（黄〜赤）と競合するため不採用。

---

## 2. レイアウト — 地図UIパターン（melta に無い、新規定義）

melta-ui はサイドバー＋テーブル型のSaaS画面前提で、全画面地図にUIを重ねる構えを持たない。ここは新規。

### PC：③開閉式パネル
- **閉じた状態**：地図が全面（没入）。探索フェーズ向き。
- **開いた状態**：固定領域でエリアカルテ・比較を読み込む（収束フェーズ向き）。melta の card/table/タイポが活きる。
- メニュー（バー/アイコン）でパネルを出し入れし、ユーザーがフェーズに応じて構えを切り替える。

#### パネルの置き方（`ADR-0029`）
開いたパネルの置き場・可動性をパネルの役割で定める。

- **絞り込みパネル（F3）＝左の操作列のボタンで開閉する、左から出るオーバーレイのドロワー（PCは固定・移動なし）**：トグルは**左の操作列のラベル付きボタン1つ**（`SlidersHorizontal`＋「絞り込み」）。**指標トグルの下**に置き、倍率表示・指標トグルと衝突させない（別レイヤーで浮かせる丸アイコンは廃止＝モーダルと重なる違和感の元）。閉＝そのボタンのみ（押すと開く）／開＝**ボタンは残り（active 表示）その直下に**左からスライドして出るパネル＋ヘッダにタイトル＋**閉じる(X)**。閉じる＝**ボタン再クリック**／X／ESC（`ADR-0031` で `ADR-0029` の「開いたらボタンを隠す・閉じはX/ESCのみ」を更新）。閉じれば地図が全面に戻る（絞り込み中も地図を主役に・`ADR-0028`）。**PCは移動なし（固定）**（任意移動は邪魔＝層4判断）。
- **絞り込みパネルのモバイル（スマホ幅）＝ボトムシート**（全幅・下から）で PC と挙動を分ける。**実装は別スライス（モバイル対応）に回す**（当面 PC のみ）。
- **カルテパネル（F5）＝右側固定**（可動化しない）：読み込む収束フェーズの安定した置き場を保つ。左=探索（左オーバーレイ）／右=収束（右固定）の左右対比。
- 開閉は**“状態”であって画面遷移ではない**（`ADR-0018`・開閉を状態として持つ）。アニメは開閉のスライドだけ＝モーション用途トークン（`--motion-base 200ms`・`prefers-reduced-motion`）に従う。

### モバイル：ボトムシート（PC実装後）
- 地図を全画面にし、UIは下から引き上げるボトムシートで重ねる。
- PCの「出し入れ」思想と一貫させる（同じメンタルモデル）。

### 地図上に配置する要素
フィルタ／検索、レイヤー切替、凡例（コア価値の防災で必須）、エリアカルテ、ピン留めリスト。
PC設計時に「各要素がモバイルでボトムシートのどこへ退避するか」を念頭に置く。

---

## 3. タイポグラフィ — 文脈で密度を変える（melta の段を用途トークンで運用）

melta の本文は 18px／行間2.0（Breathable）。地図UIの狭いパネルでは情報が詰められず窮屈になる。
**値は変えず、文脈で使い分ける**（melta は body/body-sm/caption を既に持つ）。

- **読ませる場所**（カルテの説明文・街の特徴のテキスト）→ `body`（18px/2.0）。Breathable を活かす。
- **データを詰める場所**（パネル内の数値一覧・相場/リスク/人口の一覧・ピン留めリスト）→ `body-sm`（15px/1.5前後）を既定にする。左右整列で一覧性を出す。

---

## 4. トークン設計の原則（密度切替・ダーク後付けを支える）

**コンポーネントは生の値を直書きせず、“意味・用途の名前” のトークンを参照する。** これにより密度調整もダーク対応も一箇所で効く。

- **トークンの表現＝CSS変数の3層**（`ADR-0027` で確定。Tailwind は採らない＝記録の乖離を是正）：**生値層**（`--slate-900`／`--coral-600` 等パレット）→**意味層**（`--color-text: var(--slate-900)`／`--color-accent: var(--coral-600)` ＝役割で命名）→**用途層**（共通部品は色名を書かず `var(--color-accent)` 等だけ参照）。定義は `web/src/styles/`（`:root`）に1箇所。
- **密度**：`text-data`（データ一覧用＝body-sm を指す）／`text-prose`（読ませる文章用＝body を指す）のような用途トークンを介す。後で密度を変えてもトークンの参照先を変えるだけで全画面に効く。
- **ダーク**：色は `--color-text` 等の意味トークンで参照する。`body.dark { … }` の1ブロックで意味層の中身を差し替えると cascade で全子孫に届き、部品コードは不変（MVP はライトのみ実装・`ADR-0027`）。
- **地図は別経路**：MapLibre は CSS 変数を解さないため、地図 paint 値は `web/src/styles/mapTokens.ts` が担う（どの土台でも同じ。将来 CSS 変数へ寄せ手写しを解消＝`99` 宿題）。

---

## 5. ダークモード

- **MVPは見送り（ライトのみ）。** 地図もライトスタイル前提に統一。
- **長期的には導入したい。** melta の tokens には dark 定義が既にあるため、意味トークン参照を徹底すれば後から有効化できる。MVPでは「後付けできる構造」を保つことだけを要件とする。

---

## 6. アイコン

- melta-ui の Charcoal（207）＋ Lucide（15）を継承。**描画は SVG**（melta-ui 各部品と同じ）。
- **絵文字（機種依存文字）は UI に使わない**。環境（OS/フォント）で字形・有無が変わり、意味も色も制御できず DS の外。意味を添えるアイコンは上記体系（Charcoal/Lucide・SVG）を使う。
- アイコンは**必要なときだけ**（飾りで足さない）。
- 地図UI特有（レイヤー、ピン、凡例、現在地等）で不足するアイコンの扱いは、DS調整の締めで確認（`99_decision-register.md`）。

---

## 7. モーション（動的挙動）

melta §0 のモーション継承を具体化する（スライス3.6・`ADR-0027` のトークン土台に乗せる）。

- **程度＝用途トークンで持つ**（`web/src/styles/tokens.css`・部品は数値を直書きしない）：`--motion-fast 150ms`（ホバー・フォーカスの小さな変化）／`--motion-base 200ms`（パネル開閉など）／`--motion-ease cubic-bezier(.4,0,.2,1)`。melta 作法＝**150〜300ms・控えめ**（`prohibited.md` duration-500 禁止・常時ループ禁止）。
- **`prefers-reduced-motion: reduce` は必須対応**（`prohibited.md`・melta 非交渉）。`global.css` の1箇所で transition/animation を実質無効化し、スケルトンの脈動も止める＝動きに敏感なユーザーへ即時表示。
- **ホバーと選択は色で意味を分ける**：**ホバー＝中立 near-black の縁取り**（一時の合図・触れているだけ）、**選択＝コーラル brand の縁取り**（確定・§1）。重ね順は選択が上＝ホバー中かつ選択中は確定が勝つ。差し色（コーラル）は「点」使いのまま（§1）、ホバーに差し色を使わない（一時/確定の区別と点使いを守る）。地図の縁取り色は `mapTokens.ts` に集約（CSS変数を解さない別経路・§4）。
- **地図のホバーは「面＋線が連動して反応」する**（スライス3.6・層4 目視の指摘の核）：マウスが乗った自治体だけ、**中立オーバーレイを全面に**（不透明度0.28＝層4 目視で 0.12 は淡すぎて気付けず 0.28 へ上げた・データ色は残しつつ明確に沈ませる）＋**輪郭線も中立 near-black で連動**。輪郭線は**常に通常輪郭より太く**（ズーム連動で +1.5px 以上）し、拡大しても通常線に埋もれて消えないようにする（旧・固定2pxは拡大時に負けた）。面と線は同じ feature-state `hover` で同期する。
- **ローディングはスケルトンで形を予告**（melta skeleton.md・`prohibited.md` スピナー単独の全画面禁止）：色は slate-200 固定（`--color-skeleton`）、`aria-busy`/`role=status`/sr-only を必須にする。

---

## 8. 検証（Feedback との接続）

- 生成したUIは melta の `check_html`（MCP）で DESIGN.md 規約への適合を自己検証する。
- melta-ui を取り込んで値（primary 等）を変えた場合、MCP の検証ルールとの整合を取る必要がある（`99_decision-register.md`）。
- 詳細な検証の位置づけは `04_harness.md`（Feedback 層3）。
